import React from "react";

import CountryCard from "./CountryCard";

const CountriesList = () => {
  return (
    <div>
      {/*recipes.map(recipe => (
      
    ))*/}

      <CountryCard />
    </div>
  );
};

export default CountriesList;
